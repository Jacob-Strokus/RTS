using UnityEngine;

namespace FrontierAges.Presentation {
    // Tag component: when present on a GameObject (or any parent), visual sanitizers won't disable its renderers.
    public class SanitizationExempt : MonoBehaviour {}
}
